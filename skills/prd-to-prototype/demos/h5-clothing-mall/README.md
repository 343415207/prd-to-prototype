# H5 服装商城

React + antd-mobile + Agentation 构建 single HTML，支持 `file://` 离线打开。

## 运行

```bash
# 安装依赖
npm install

# 开发预览（需联网）
npm run dev

# 构建 single HTML（输出到 dist/index.html）
npm run build
```

## 验证

构建完成后，直接双击打开 `dist/index.html`，应可离线使用（商品图片仍来自 picsum.photos，需联网）。
